import { colors } from './colors'
import { spacing } from './spacing'
import { textVariants } from './text'
import { shadows } from './shadows'

export const useAppTheme = () => {
  return { colors, spacing, textVariants, shadows }
}
